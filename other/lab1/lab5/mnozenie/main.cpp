#include <iostream>

int squareInt(double a) {
    return a*a;
}

double squareDouble(double a) {
    return a*a;
}

int main() {
    /*
    for (int i = 0; i < 10; ++i) {
        for (int j = 0; j < 10; ++j) {
            std::cout << i+1 << "*" << j+1 << " = " << (i+1)*(j+1) << "\n";
        }
        std::cout << std::endl;
    }

    
    for (int i = char('A'); i < char('A') + 26; ++i)
        std::cout << char(i);
    std::cout << std::endl;
    for (int i = char('Z'); i > char('Z') - 26; --i)
        std::cout << char(i);
    std::cout << std::endl;
     */
    return 0;
}
